<?xml version="1.0" encoding="utf-8"?>

<!--
This is the master template for a standard dialog.

Usage:

  <Include
      xmlns="www.team-mediaportal.com/2008/mpf/directx"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Source="screens\master_dialog.xaml"
      >
    <Include.Resources>
      <ResourceWrapper x:Key="Focused_Element">
        [Reference of the focused element (or null)]
      </ResourceWrapper>
      <ResourceWrapper x:Key="Dialog_Header_Text">
        [Header text to be shown as dialog title]
      </ResourceWrapper>
      <ResourceWrapper x:Key="Contents_Element">
        [Element which represents the contents area of the dialog.
         Will be placed into a ScrollViewer.]
      </ResourceWrapper>
    </Include.Resources>
  </Include>

Parameters which have to be accessible by a DynamicResource lookup:
 - Focused_Element: An element which should be focused
 - Dialog_Header_Text (String): The text to be displayed as dialog title
 - Contents_Element (Any UI element): Contents area of the dialog.
-->

<Canvas
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    FocusedElement="{DynamicResource Focused_Element}">

  <Border Canvas.Left="50" Canvas.Top="44" Width="620" Height="470" Background="{StaticResource DialogBorderColor}" CornerRadius="10">
    <Canvas Width="600" Height="450" VerticalAlignment="Center" HorizontalAlignment="Center" Background="{StaticResource DialogBackgroundBrush}">
      <Label FontSize="30" Canvas.Left="25" Canvas.Top="0" Content="{DynamicResource Dialog_Header_Text}" Width="400" Color="{StaticResource DialogHeaderColor}" VerticalAlignment="Top"/>
      <Button x:Name="CloseBtn" Canvas.Left="545" Canvas.Top="5" Style="{StaticResource DialogCloseButtonStyle}"
          Command="{Command Source={Service ScreenManager},Path=CloseDialog}"/>
      <ContentPresenter x:Name="ContentPanel" Canvas.Left="34" Canvas.Top="50" Height="380" Width="500" Content="{DynamicResource Contents_Element}"/>
    </Canvas>
  </Border>
</Canvas>
