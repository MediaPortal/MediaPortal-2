<Window x:Class="MP2BootstrapperApp.Views.StartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MP2BootstrapperApp.Views"
        mc:Ignorable="d"
        Title="MP2 Setup" Height="350" Width="525">
  <Window.Resources>
    <Style TargetType="{x:Type Button}">
      <Setter Property="Margin" Value="15"/>
      <Setter Property="Height" Value="25"/>
      <Setter Property="Width" Value="70" />
    </Style>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <DockPanel Grid.Column="0" Grid.Row="0">
      <Image Source="..\resources\MP2Icon184.jpg" Width="184" Height="184" HorizontalAlignment="Left"/>
      <StackPanel>
        <TextBlock TextWrapping="Wrap" FontSize="20">Welcome to the Media Portal 2 Setup Wizard</TextBlock>
        <TextBlock TextWrapping="Wrap" Margin="0,40,0,0" FontSize="14">The Setup Wizard will install Media Portal 2 on your computer. Click Next to continue or Cancel to exit the Setup Wizard</TextBlock>
      </StackPanel>
    </DockPanel>
    <DockPanel Grid.Row="1" HorizontalAlignment="Right"  VerticalAlignment="Bottom" >
      <Button Content="Back" />
      <Button Content="Next" />
      <Button Content="Cancel" />
    </DockPanel>
  </Grid>
</Window>
