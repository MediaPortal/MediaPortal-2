<?xml version="1.0" encoding="utf-8"?>
<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:collections="clr-namespace:MediaPortal.Presentation.Collections"
    Source="screenfiles\master_menu.xaml"
    >
  <Include.Resources>

    <!-- Header -->
    <ResourceWrapper x:Key="Header_Text" Resource="[SharesConfig.RemoveShares]"/>

    <!-- Shares configuration model -->
    <Model x:Key="Model" Id="1768FC91-86B9-4f78-8A4C-E204F0D51502"/>

    <!-- Contents -->
    <ResourceWrapper x:Key="Contents_Element">
      <Grid Margin="10,0,10,10">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListView Grid.Row="0"
            Style="{StaticResource MarkableSharesListViewStyle}"
            DataContext="{Binding Source={StaticResource Model}}"
            ItemsSource="{Binding Shares}"/>
        <Grid Grid.Row="1">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Button x:Name="RemoveButton" Margin="20,10,0,0" Height="40" Style="{StaticResource ButtonStyle}"
              Content="[SharesConfig.RemoveSelectedShares]" HorizontalAlignment="Right">
            <Button.Command>
              <CommandList>
                <Command Source="{StaticResource Model}" Path="RemoveSelectedShares"/>
                <Command Source="{Service WorkflowManager}" Path="NavigatePop" Parameters="1"/>
              </CommandList>
            </Button.Command>
          </Button>
        </Grid>
      </Grid>
    </ResourceWrapper>

  </Include.Resources>
</Include>
