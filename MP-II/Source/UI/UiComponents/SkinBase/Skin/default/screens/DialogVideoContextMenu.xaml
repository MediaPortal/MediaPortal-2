<?xml version="1.0" encoding="utf-8"?>
<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Source="screens\master_dialog_menu.xaml"
    >
  <Include.Resources>

Weiter. Achtung: master_dialog_menu schließt Dialog, nachdem er das Button-Kommando gefeuert hat. Wenn wir im Button-Kommando den
Zoom-Dialog anzeigen, wird der sofort wieder geschlossen...

    <!-- Header -->
    <ResourceWrapper x:Key="Dialog_Header_Text" Resource="[movies.5]"/>

    <!-- DataContext -->
    <Model x:Key="DataContext" RegistrationLocation="Movies" Id="Model"/>

    <!-- Menu -->
    <ResourceWrapper x:Key="Menu_Items">
      <Binding Source="{StaticResource DataContext}" Path="OSD.ContextMenu" Mode="OneTime"/>
    </ResourceWrapper>

    <!-- Command -->
    <ResourceWrapper x:Key="Execute_Command_Stencil">
      <!-- TODO -->
    </ResourceWrapper>

  </Include.Resources>
</Include>
