<?xml version="1.0" encoding="utf-8"?>

<!--
This is the master template for all screens:
A header text and a client area.

Usage:

  <Include
      xmlns="www.team-mediaportal.com/2008/mpf/directx"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Source="screenfiles\master.xaml">
    <Include.Resources>
      <ResourceWrapper x:Key="Header_Text">
        [Header text]
      </ResourceWrapper>
      <ResourceWrapper x:Key="Client_Element">
        [UI element to be displayed in the client area]
      </ResourceWrapper>
      <ResourceWrapper x:Key="Focused_Element">
        [UI element to be focused]
      </ResourceWrapper>
    </Include.Resources>
  </Include>

Parameters which have to be accessible by a DynamicResource lookup:
 - Header_Text (String): The text to be displayed as header
 - Focused_Element (Any existing UI element): Element to be focused at startup
 - Client_Element (Any UI element): Element to be shown in the client area
-->

<Grid
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    FocusedElement="{DynamicResource Focused_Element}"
    Context="{DynamicResource DataContext}"
    >
  <DockPanel Background="{StaticResource BackgroundBrush}" Width="720" Height="576" LastChildFill="True">
    <Grid x:Name="Grid1" DockPanel.Dock="Top" HorizontalAlignment="Left">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>  
      <StackPanel x:Name="HeaderPanel" Orientation="Vertical" Grid.Column="0" Grid.Row="0">
        <Label Margin="60,0,0,0" FontFamily="RitzyNormal" FontSize="60" Content="{DynamicResource Header_Text}" />
        <Label Margin="60,0,0,0" DockPanel.Dock="Top" FontFamily="RitzyNormal" FontSize="60" Content="{DynamicResource Header_Text}"
            Opacity="0.75" RenderTransformOrigin="0.4,0.4">
          <Label.RenderTransform>
            <TransformGroup>
              <ScaleTransform ScaleX="1" ScaleY="-0.75"/>
            </TransformGroup>
          </Label.RenderTransform>
          <Label.OpacityMask>
            <LinearGradientBrush StartPoint="0.5,0.0" EndPoint="0.5,1.0">
              <GradientStop Offset="0.0" Color="#FF000000" />
              <GradientStop Offset="1.0" Color="#00000000" />
            </LinearGradientBrush>
          </Label.OpacityMask>
        </Label>
      </StackPanel>
      <StackPanel Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top">
        <Label x:Name="Date" FontSize="20" Content="{Binding Source={Service ScreenManager}, Path=TimeUtils.CurrentDate}" />
        <Label x:Name="Time" FontSize="20" Content="{Binding Source={Service ScreenManager}, Path=TimeUtils.CurrentTime}" />
	    </StackPanel>
    </Grid>

    <StackPanel x:Name="ClientPanel" DockPanel.Dock="Center" Margin="10,0,0,0" VerticalAlignment="Top" HorizontalAlignment="Left">
      <ContentPresenter Content="{DynamicResource Client_Element}"/>
    </StackPanel>
  </DockPanel>
</Grid>
