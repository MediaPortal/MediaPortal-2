<?xml version="1.0" encoding="utf-8"?>
<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Source="screenfiles\master_dialog_menu.xaml"
    >
  <Include.Resources>

    <!-- Header -->
    <ResourceWrapper x:Key="Dialog_Header_Text" Resource="[playback.3]"/>

    <!-- ListView style -->
    <ResourceWrapper x:Key="ListViewStyle" Resource="{StaticResource MenuListViewStyle}"/>

    <!-- Menu -->
    <ResourceWrapper x:Key="Menu_Items">
      <Binding Source="{Service Players}" Path="Settings.DefaultSubtitleLanguages" Mode="OneTime"/>
    </ResourceWrapper>

    <!-- Command -->
    <ResourceWrapper x:Key="Execute_Command_Stencil">
      <CommandStencil Source="{Model RegistrationLocation=Settings,Id=VideoSettings}" Path="Players.Settings.SetDefaultSubtitleLanguage"/>
    </ResourceWrapper>

  </Include.Resources>
</Include>
