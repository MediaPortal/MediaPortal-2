<?xml version="1.0" encoding="utf-8"?>

<!--
This is the master template for standard menu screens:
A header text, left the menu, under the menu a small video preview,
at the right side the content pane and at the bottom the current play control.

Usage:

  <Include
      xmlns="www.team-mediaportal.com/2008/mpf/directx"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Source="screens\master_menu.xaml">
    <Include.Resources>
      <ResourceWrapper x:Key="Header_Text">
        [Header text]
      </ResourceWrapper>
      <ControlTemplate x:Key="Contents_Template">
        [UI element to be displayed in the content area]
      </ControlTemplate>
    </Include.Resources>
  </Include>

Parameters which have to be accessible by a DynamicResource lookup:
 - Header_Text (String): The text to be displayed as header
 - Contents_Template (ConrolTemplate): Template for element to be displayed
   in the content area at the right side
-->

<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Source="screens\master_home.xaml"
    xmlns:mp_special_controls="clr-namespace:MediaPortal.UI.SkinEngine.SpecialElements.Controls"
    >
  <Include.Resources>

    <!-- Header has to be defined by the includer: "Header_Text" -->

    <!-- Client_Template -->
    <ControlTemplate x:Key="Client_Template">
      <!--<DockPanel LastChildFill="True" Margin="10">-->
      <Grid x:Name="MainLayoutPanel" Margin="10,0,0,10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
          <Model x:Key="MenuModel" Id="9E9D0CD9-4FDB-4c0f-A0C4-F356E151BDE0"/>
        </Grid.Resources>

        <Canvas x:Name="Text_bar" Margin="-150,0,0,0"  Opacity="0.902" Grid.Column="0" Grid.Row="0" Grid.RowSpan="3">
          <Image x:Name="Layer_4" Height="126" Canvas.Left="0" Opacity="0.749" Source="Controls\Layer 141.png" Canvas.Top="326" Width="2200"/>
          <Image x:Name="Layer_14_copy" Height="126" Canvas.Left="0" Opacity="0.788" Source="Controls\Layer 14 copy.png" Canvas.Top="322" Width="2200"/>
          <Image x:Name="Layer_3" Height="134" Canvas.Left="0" Source="Controls\Layer 11.png" Canvas.Top="318" Width="2200"/>
          <Image x:Name="Layer_2" Height="133" Canvas.Left="0" Opacity="0.29" Source="Controls\Layer 51.png" Canvas.Top="319" Width="2200"/>
          <Image x:Name="Layer_22" Height="100" Canvas.Left="0" Opacity="0.8" Source="Controls\Layer 22.png" Canvas.Top="335" Width="2200"/>
        </Canvas>

        <ListView x:Name="MainMenu" Grid.Column="1" Grid.Row="1" Style="{ThemeResource HomeMenuListViewStyle}"
              HorizontalAlignment="Stretch" VerticalAlignment="Center" SetFocusPrio="DefaultHigh"
              ItemsSource="{Binding Source={StaticResource MenuModel},Path=MenuItems,Mode=OneTime}">
          <ListView.Resources>
            <CommandBridge x:Key="Menu_Command" Command="{Binding Path=Command,Mode=OneTime}"/>
          </ListView.Resources>
        </ListView>

        <!-- Order matters here: The content grid has to be the last control to fill the rest of the space -->
        <!--<Grid x:Name="ContentPanel" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" SetFocusPrio="Default">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>
          <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Control Margin="10,0,10,10" x:Name="ContentViewer" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
              Template="{DynamicResource ResourceKey=Contents_Template}" SetFocus="true"/>
        </Grid>-->
      </Grid>
      <!--</DockPanel>-->
    </ControlTemplate>

  </Include.Resources>
</Include>
