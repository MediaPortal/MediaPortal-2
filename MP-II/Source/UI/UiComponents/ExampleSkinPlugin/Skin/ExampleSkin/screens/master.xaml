<?xml version="1.0" encoding="utf-8"?>

<!--
This is the master template for all screens:
A header text and a client area.

Usage:

  <Include
      xmlns="www.team-mediaportal.com/2008/mpf/directx"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Source="screens\master.xaml">
    <Include.Resources>
      <ResourceWrapper x:Key="Header_Text">
        [Header text]
      </ResourceWrapper>
      <ResourceWrapper x:Key="Client_Element">
        [UI element to be displayed in the client area]
      </ResourceWrapper>
      <ResourceWrapper x:Key="Focused_Element">
        [UI element to be focused]
      </ResourceWrapper>
    </Include.Resources>
  </Include>

Parameters which have to be accessible by a DynamicResource lookup:
 - Header_Text (String): The text to be displayed as header
 - Focused_Element (Any existing UI element): Element to be focused at startup
 - Client_Element (Any UI element): Element to be shown in the client area
-->

<Grid
	xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	x:Name="BackgroundGrid">
	
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>  
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>  
    <Control Style="{ThemeResource BackgroundControlStyle}"/>

	<DockPanel
	    FocusedElement="{DynamicResource Focused_Element}"
	    Context="{DynamicResource DataContext}"
	    LastChildFill="True">
	  <Grid x:Name="HeaderPanel" DockPanel.Dock="Top">
	    <Grid.Resources>
	      <Model x:Key="TimeModel" Id="E821B1C8-0666-4339-8027-AA45A4F6F107"/>
	    </Grid.Resources>
	    <Grid.ColumnDefinitions>
	      <ColumnDefinition Width="*"/>
	      <ColumnDefinition Width="Auto"/>
	    </Grid.ColumnDefinitions>  
	    <StackPanel x:Name="HeaderTextPanel" Orientation="Vertical" Grid.Column="0" Grid.Row="0">
	      <Label Margin="30,10,0,0" FontFamily="SegoeUI" FontSize="48" Color="{StaticResource WindowHeaderColor}"
	          Content="{DynamicResource Header_Text}" />
	      <Label Margin="30,-5,0,0" DockPanel.Dock="Top" FontFamily="SegoeUI" FontSize="48" Color="{StaticResource WindowHeaderColor}"
	          Content="{DynamicResource Header_Text}"
	          Opacity="0.1" RenderTransformOrigin="0.4,0.4">
	        <Label.RenderTransform>
	          <TransformGroup>
	            <ScaleTransform ScaleX="1" ScaleY="-1"/>
	          </TransformGroup>
	        </Label.RenderTransform>
	        <Label.OpacityMask>
	          <LinearGradientBrush StartPoint="0.5,0.0" EndPoint="0.5,1.0">
	            <GradientStop Offset="0.0" Color="#AA000000" />
	            <GradientStop Offset="0.4" Color="#00000000" />
	          </LinearGradientBrush>
	        </Label.OpacityMask>
	      </Label>
	    </StackPanel>
	    <StackPanel x:Name="DateTimePanel" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top">
	      <Label Margin="0,10,0,0" x:Name="Date" FontSize="20" Content="{Binding Source={Model E821B1C8-0666-4339-8027-AA45A4F6F107}, Path=CurrentDate}" />
	      <Label Margin="0,0,0,0" x:Name="Time" FontSize="20" Content="{Binding Source={Model E821B1C8-0666-4339-8027-AA45A4F6F107}, Path=CurrentTime}" />
	    </StackPanel>
	  </Grid>
	  <ContentPresenter x:Name="ClientPresenter" DockPanel.Dock="Center" Margin="10,0,0,0"
	      Content="{DynamicResource Client_Element}"/>
	</DockPanel>
</Grid>
