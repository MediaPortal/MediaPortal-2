<?xml version="1.0" encoding="utf-8"?>
<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Source="screens\master_menu.xaml"
    xmlns:mp_special="clr-namespace:MediaPortal.UI.SkinEngine.SpecialElements.Controls"
    >
  <Include.Resources>

    <ResourceWrapper x:Key="Header_Text" Resource="[Media.AudioCurrentlyPlayingHeader]"/>

    <ResourceWrapper x:Key="Contents_Element">
      <DockPanel x:Name="RightPanel" Margin="20" LastChildFill="true">
        <DockPanel.Context>
          <mp_special:PlayerControl
              SlotIndex="{Binding Source={Model Id=D8998340-DA2D-42be-A29B-6D7A72AEA2DC},Path=CurrentPlayerIndex}"/>
              <!-- Model = Audio player model -->
        </DockPanel.Context>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
          <Label x:Name="ArtistLabel" Content="[Media.AudioArtistLabel]"/>
          <Label x:Name="Artist" Content="{Binding AudioArtist}"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
          <Label x:Name="TitleLabel" Content="[Media.AudioTitleLabel]"/>
          <Label x:Name="Title" Scroll="true" Content="{Binding MediaItemTitle}"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
          <Label x:Name="YearLabel" Content="[Media.AudioYearLabel]"/>
          <Label x:Name="Year" Content="{Binding AudioYear}"/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"
            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
          <Label x:Name="DurationLabel" Content="[Media.AudioDurationLabel]"/>
          <Label x:Name="Duration" Content="{Binding Duration}"/>
        </StackPanel>
      </DockPanel>
    </ResourceWrapper>

  </Include.Resources>
</Include>
