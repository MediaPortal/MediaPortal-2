<?xml version="1.0" encoding="utf-8"?>
<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Source="screens\master_menu.xaml"
    >
  <Include.Resources>

    <!-- Header -->
    <ResourceWrapper x:Key="Header_Text" Resource="[Media.ReimportMediaItem.Title]"/>

    <!-- Login model -->
    <Model x:Key="Model" Id="692FA8C3-41A5-43DD-8C12-C857C9C75E72"/>

    <!-- Contents -->
    <ControlTemplate x:Key="Contents_Template">
      <DockPanel DataContext="{Binding Source={StaticResource Model}}">
        <!-- Match list -->
        <ListView Margin="10" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SetFocus="True"
                    Style="{ThemeResource MenuListViewStyle}" RestoreFocus="True"
                    ItemsSource="{Binding Path=MatchList}" >
          <ListView.Resources>
            <Command x:Key="Menu_Command" Source="{StaticResource Model}" Path="SelectMatch"
                       Parameters="{LateBoundValue BindingValue={Binding}}"/>
          </ListView.Resources>
        </ListView>
      </DockPanel>
    </ControlTemplate>

  </Include.Resources>
</Include>
